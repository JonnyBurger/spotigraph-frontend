import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
	border-top: 1px solid #222;
`;

const Credit = styled.div``;

export default () => {
	return (
		<Wrapper>
			<Credit>Search p</Credit>
		</Wrapper>
	);
};
